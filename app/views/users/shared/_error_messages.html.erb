<% if resource && resource.errors.any? %>
  <div class="alert alert-danger">
    <h4 class="alert-heading">入力にエラーがあります:</h4>
    <ul>
      <% resource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>